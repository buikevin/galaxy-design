# @galaxy-ui/visual-tests

Visual regression testing for Galaxy UI components using Playwright.

## Quick start

```bash
cd packages/visual-tests
bun install
bun run test           # run all visual tests
bun run update         # update baseline screenshots
bun run report         # view HTML report
```

Requires the docs playground running at localhost:5173 (auto-started by config).
